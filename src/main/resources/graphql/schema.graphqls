type Query {
    getEquipmentById(id: ID!): Equipment
}

type Mutation {
    createEquipment(
        name: String!
        manufacturer: String!
        description: String!
        ownerId: ID
    ): Equipment
    createInventory: String
    deleteInventory(id: ID!): Boolean
}
